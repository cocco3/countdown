const SIZE = '36px'

export default {
  Avatar: {
    alignItems: 'center',
    backgroundColor: '#ffffff',
    borderRadius: '50%',
    color: '#282c34',
    display: 'inline-flex',
    height: SIZE,
    lineHeight: SIZE,
    justifyContent: 'center',
    width: SIZE
  }
}
